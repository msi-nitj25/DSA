// all same as map but it does not store elements in sorted order.
// it will have unique keys 
// map work in log n time complexity , while Unordered map work in O(1) time complexity on average case.
// and O(n) in worst case due to hash collision, but it is very rare and can be avoided by using good hash functions.
