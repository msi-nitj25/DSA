// same as sets but its not ordered . Its store unique values only but not in sorted order
#include<bits/stdc++.h>
using namespace std;
void expalinUSet(){
    // upper and lower bound function are not present in unordered set because it does not store elements in sorted order
    // all other functions are same as set. time complexity of all functions is O(1) in unordered set, while it is O(log n) in set
    // some time its time complexity can be O(n) in unordered set in case of hash collision, but it is very rare and can be avoided by using good hash functions.
    
}